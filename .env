# App
PORT=3000
NODE_ENV=development
# Replace with a secure secret in your local dev or CI (do NOT commit real secrets)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# Database
# Replace placeholders with your local DB credentials
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nodepoc?schema=public"
